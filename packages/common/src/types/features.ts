export type FeatureFlags = {
  vendor_bulk_upload: boolean;
  vendor_video_upload: boolean;
  vendor_withdrawals: boolean;
  vendor_shipping_management: boolean;
  vendor_product_auto_approval: boolean;
  allow_vendor_theme_override: boolean;
  product_sequence_enabled: boolean;
  auto_send_whatsapp: boolean;
  analytics_enabled: boolean;
  enable_meta_pixel: boolean;
  enable_server_side_analytics: boolean;
  allow_vendor_download_template: boolean;
  vendor_chat: boolean;
};
